---
const titulo = "Bienvenidos a Trazas Ingeniería";
const descripcion = `
  En TRAZAS INGENIERÍA, no solo procesamos metales, sino que
  también construimos relaciones duraderas con nuestros clientes,
  basadas en la confianza y el compromiso con la excelencia.
`;

// Ajusta la ruta de la imagen a la que tengas en tu proyecto
const imagenFondo = "/assets/solar.jpg";
---

<section 
  class="relative bg-cover bg-center flex items-center justify-center py-24 px-4 md:px-8"
  style={`background-image: url(${imagenFondo});`}
>
  <!-- Capa semitransparente para oscurecer la imagen de fondo -->
  <div class="absolute inset-0 bg-black/40"></div>

  <!-- Contenido principal -->
  <div class="relative z-10 max-w-3xl text-center text-white">
    <h1 class="text-4xl md:text-5xl font-bold mb-6">
      {titulo}
    </h1>
    <p class="text-lg md:text-xl mb-8 leading-relaxed">
      {descripcion}
    </p>
    <a 
      href="/servicios" 
      class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-medium px-6 py-3 rounded transition-colors"
    >
      Empecemos
    </a>
  </div>
</section>

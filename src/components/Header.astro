---
import { getCollection } from "astro:content";

import HeaderSvelte from "@svelte/HeaderSvelte.svelte";

const posts = await getCollection("info");

const Posts = posts.map((post:any) => {
  return {
    id: post.data.id,
    slug: post.slug,
  };
});
---

<HeaderSvelte Posts={Posts} client:load />


